# 求解二叉树的最大深度：
方法：
- 不要一开始就陷入细节，而是思考整棵树与左右子树的关系：
-   最大深度等于左右子树最大深度的max值 + 1
```java
max(root.left, root.right) + 1
```

注意：递归过程中的问题拆解以及边界条件

# 判断二叉树是否为二叉搜索树
二叉搜索树：
- 定义：对所有节点----左子树的值 < 根节点的值 < 右子树的值
- 性质：
-   中序遍历结果一定是升序序列。
-   查找、插入、删除操作在“平衡”情况下时间复杂度为 O(log n)，最坏退化成链时为 O(n) 